node_modules
.next
.env*
# Pero sí queremos publicar la plantilla:
!.env.example
# Base de datos local SQLite (vive en /data/tareas.db).
# IMPORTANTE: usar / inicial para anclar a la raíz; "data/" sin slash
# matchea cualquier carpeta "data" en cualquier nivel del proyecto
# (p.ej. components/data/) — eso me mordió una vez ya.
/data/
/data/*.db
/data/*.db-shm
/data/*.db-wal
/data/*.db-journal
*.tsbuildinfo
next-env.d.ts
.vercel
# Contexto privado del proyecto (credenciales, IDs internos)
CLAUDE.md
